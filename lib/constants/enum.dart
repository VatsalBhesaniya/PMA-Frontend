enum HttpMethod { post, get, put, delete, patch }

enum InputFieldHeight {
  small,
  large,
}

enum InputFieldBorderType {
  none,
  outlineInputBorder,
  underlineInputBorder,
}

enum MemberRole {
  owner,
  admin,
  member,
  guest,
}

enum MemberStatus {
  accepted,
  invited,
}
